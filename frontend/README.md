# frontend

Emby Episode Organizer 前端骨架。

## 约定

- Vite 5 + Vue 3 + TypeScript
- Naive UI 统一通过 `NConfigProvider` 包裹
- Pinia 用于全局状态
- `/api` 走本地代理到 `http://localhost:8899`

## 脚本

- `npm run dev`
- `npm run build`
- `npm run test:unit`
- `npm run lint`
- `npm run format`
